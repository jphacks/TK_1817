<p>Hello, world!</p>

<%= form_tag search_path, method: :get do %>

    <div class="input-group">
        <%= text_field :search, :playlist_user, class: 'form-control', type: 'search', placeholder: 'プレイリストのユーザー', aria: { label: 'Search' } %>
        <%= text_field :search, :playlist_id, class: 'form-control', type: 'search', placeholder: 'プレイリストのID', aria: { label: 'Search' } %>
        <%= submit_tag '分析', name: nil  %>
    </div>
<% end %>
<%= link_to 'Sign in with Spotify', '/auth/spotify' %>;
